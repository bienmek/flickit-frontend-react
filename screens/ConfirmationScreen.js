import {useObjectContext} from "../context/objectContext";


export default function ConfirmationScreen ({navigation}) {

    const {takenFlick, currentObject} = useObjectContext()

    return (
        <>
        </>
    )
}